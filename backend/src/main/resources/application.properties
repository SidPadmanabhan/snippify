spring.application.name=snippify
# server configuration
server.port=8080


# Database Configuration (commented out until PostgreSQL is set up)
# spring.datasource.url=jdbc:postgresql://localhost:5432/snippify_db
# spring.datasource.username=postgres
# spring.datasource.password=your_password
# spring.datasource.driver-class-name=org.postgresql.Driver
# JPA/Hibernate Configuration (commented out until PostgreSQL is set up)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.format_sql=true